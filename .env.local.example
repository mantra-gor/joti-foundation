NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Server-only — used by the /api/contact, /api/volunteer, and /api/newsletter
# Route Handlers (src/lib/mailer.js) to send mail via Gmail SMTP.
# GMAIL_APP_PASSWORD is a Google Account app password, not your login password:
# https://myaccount.google.com/apppasswords
GMAIL_USER=
GMAIL_APP_PASSWORD=
CONTACT_TO_EMAIL=
